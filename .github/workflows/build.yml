name: Build GLR Collector

on:
  push:
    branches: [ "main" ]
    tags: [ "v*" ]
  workflow_dispatch:

# Grant permission to create releases
permissions:
  contents: write

jobs:
  build:
    name: Build macOS App
    runs-on: macos-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # STAP 1: Install Platypus (The Official Way)
      - name: Install Platypus
        uses: sveinbjornt/setup-platypus@master
        with:
          version: 'latest'

      # STAP 2: Generate Apple Icon (.icns)
      - name: Generate Icon
        run: |
          mkdir MyIcon.iconset
          sips -z 16 16     app.ico --out MyIcon.iconset/icon_16x16.png
          sips -z 32 32     app.ico --out MyIcon.iconset/icon_16x16@2x.png
          sips -z 32 32     app.ico --out MyIcon.iconset/icon_32x32.png
          sips -z 64 64     app.ico --out MyIcon.iconset/icon_32x32@2x.png
          sips -z 128 128   app.ico --out MyIcon.iconset/icon_128x128.png
          sips -z 256 256   app.ico --out MyIcon.iconset/icon_128x128@2x.png
          sips -z 256 256   app.ico --out MyIcon.iconset/icon_256x256.png
          sips -z 512 512   app.ico --out MyIcon.iconset/icon_256x256@2x.png
          sips -z 512 512   app.ico --out MyIcon.iconset/icon_512x512.png
          sips -z 1024 1024 app.ico --out MyIcon.iconset/icon_512x512@2x.png
          iconutil -c icns MyIcon.iconset

      # STAP 3: Build Application
      # We use the flags we defined earlier:
      # -D : Droppable (Crucial for drag & drop support)
      # -o 'None' : No text window (Since we use native GUI dialogs)
      # -i : Icon path
      # -I : Bundle ID
      - name: Build App
        run: |
          chmod +x assignment-collector.sh
          
          platypus \
            -D \
            -i 'MyIcon.icns' \
            -y \
            -a 'GLR Collector' \
            -I 'nl.glr.assignmentcollector' \
            'assignment-collector.sh' \
            'GLR Collector.app'

      # STAP 4: Zip Application (Preserves permissions)
      - name: Zip Application
        run: zip -r GLR_Collector.zip "GLR Collector.app"

      # STAP 5: Upload Artifact (For testing)
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: GLR-Collector-Build
          path: GLR_Collector.zip
          retention-days: 5

      # STAP 6: Create Release (Only on tag push)
      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: GLR_Collector.zip